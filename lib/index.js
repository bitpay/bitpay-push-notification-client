/**
 * A client library for the BitPay push notification service.
 * @module Client
 */

/**
 * Client API.
 * @alias module:Client.API
 */
var client = module.exports = require('./api');

client.Utils = require('./common/utils');
